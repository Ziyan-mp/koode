import 'package:flutter/material.dart';

class AdminComplaintCard extends StatelessWidget {
  const AdminComplaintCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Card(
      child: ListTile(
        title: Text('Admin Complaint Card'),
      ),
    );
  }
}
