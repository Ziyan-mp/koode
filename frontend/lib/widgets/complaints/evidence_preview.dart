import 'package:flutter/material.dart';

class EvidencePreview extends StatelessWidget {
  const EvidencePreview({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}
