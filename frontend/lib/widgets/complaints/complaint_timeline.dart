import 'package:flutter/material.dart';

class ComplaintTimelineWidget extends StatelessWidget {
  const ComplaintTimelineWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}
