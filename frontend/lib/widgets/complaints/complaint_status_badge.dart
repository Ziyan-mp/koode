import 'package:flutter/material.dart';

class ComplaintStatusBadge extends StatelessWidget {
  const ComplaintStatusBadge({super.key});

  @override
  Widget build(BuildContext context) {
    return const Chip(label: Text('Status'));
  }
}
